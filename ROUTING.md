# أدلة التوجيه والتصدير (Routing & Export)

## 1. جدول استدعاء المتون (Matn Routing Table)

يُستدعى المتن بناءً على الكلمات الدلالية في عنوان المحاضرة:

### النمط الأول: الذاكرة الشاملة (Shamela)
* `(زاد)` -> زاد المعاد في هدي خير العباد (ابن القيم).
* `(صيد)` -> صيد الخاطر (ابن الجوزي).
* `(رياض)` -> رياض الصالحين (النووي / مع الاستئناس بدليل الفالحين).
* `(سيرة)` -> الرحيق المختوم (المباركفوري / طبعة الأوقاف القطرية).
* `(ديوان)` -> ديوان الإمام الشافعي.
* `(توحيد)` أو `(فتح الباري)` -> فتح الباري (كتاب التوحيد) (ابن حجر).
* `(روضة)` أو `(شرح مختصر الروضة)` -> شرح مختصر الروضة.

### النمط الثاني: ملفات مرفوعة (Uploaded Docs)
* `(لب)` أو `(أصول)` أو `(لب الأصول)` -> كتاب لب الأصول.
* `(معاملات)` أو `(دليل)` -> دليل الطالب (كتاب البيع).
* `(أسماء)` -> كتب الأسماء والصفات والعقيدة [الترويسة: الأسماء الحسنى].

## 2. خريطة التصدير (Export Routing)

تُصدَّر مخرجات التفريغ تلقائياً كملفي Word:

### أ. نسخة المستخدم الأساسية (OneDrive)
- **المسار الجذري**: `C:\Users\L\OneDrive\1. دوري`
- **التسمية**: `[YYYY-MM-DD].docx`
- **مجلدات المواد الحصرية**:
  1. `أسماء` -> `1. الاسماء الحسنى`
  2. `رياض` -> `1. رياض الصالحين`
  3. `سيرة` -> `1. سيرة (الرحيق المختوم)`
  4. `معاملات` / `دليل` -> `1. معاملات`
  5. `توحيد` / `فتح الباري` -> `2. التوحيد`
  6. `لب` / `أصول` -> `2. لب الاصول`
  7. `ديوان` -> `ديوان الشافعي`
  8. `زاد` -> `زاد المعاد`
  9. `روضة` -> `شرح مختصر الروضة`
  10. `صيد` -> `صيد الخاطر`
  - للمواد الجديدة خارج هذه القائمة: يُنشأ مجلد باسم المادة داخل `1. دوري`.
- **سير عمل التدريب**: ينقل المستخدم الملف المراجع إلى مجلد `(تم التسليم)`. تستخرج الأداة الآلية النصوص وتحفظها كملف `[Date]_[Subject].md` للتدريب في `04_Training_Data`.

### ب. نسخة الأرشيف الداخلي
- **المسار**: `03_AI_Outputs/[YYYY-MM-DD]_[اسم_المادة].docx`
- الغرض: الاستخدام الداخلي في حلقة التعلم (Diff Loop).
