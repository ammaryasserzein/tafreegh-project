# سياق مشروع تفريغ وتحقيق الدروس الشرعية (الدوري / The Curriculum)

## 1. هوية المشروع والمحاضر
- **المحاضر**: فضيلة الشيخ أسامة (نادراً ما يذكر اسمه صراحة).
- **المنهج والعلماء المكثر من النقل عنهم**: أئمة السلف، وبخاصة شيخ الإسلام ابن تيمية والإمام ابن القيم.
- **طبيعة العمل**: تفريغ مسودات Gemini Notebook، مطابقة متون، استدراك السقط، توحيد العامية المصرية، وإعداد مخرجات مطبعية لبرنامج Microsoft Word.

## 2. هيكلة المجلدات (The 4-Folder Architecture)
يعتمد المشروع على 4 مجلدات رئيسية باللغة الإنجليزية لتنظيم دورة حياة البيانات:
1. `01_Matn_Sources`: يحتوي على ملفات المتون الشرعية الأصلية المعتمدة (مثل زاد المعاد، صيد الخاطر) بصيغة Markdown لاستخدامها كمرجع موثوق للـ Blind Literalism.
2. `02_Raw_Inputs`: يحتوي على مسودات التفريغ الخام المستخرجة من Gemini Notebook قبل معالجتها.
3. `03_AI_Outputs`: الأرشيف الداخلي للمشروع؛ تُحفظ فيه المخرجات الأولية للذكاء الاصطناعي كملفات Word (`.docx`) وملفات `.md` خام للرجوع إليها.
4. `04_Training_Data`: مساحة التدريب النهائية لملفات `.md` عالية الجودة.

## 3. توجيه المتون (Routing Map)
بناءً على الكلمات الدلالية في عنوان المحاضرة، يُستدعى المتن وفق الجدول التالي (تُدار مسارات التصدير آلياً عبر البيئة):

| الكلمة الدلالية | المتن المعتمد |
|---|---|
| `(زاد)` | **زاد المعاد في هدي خير العباد** (للإمام ابن القيم) |
| `(صيد)` | **صيد الخاطر** (للإمام ابن الجوزي) |
| `(رياض)` | **رياض الصالحين** (للإمام النووي) |
| `(سيرة)` | **الرحيق المختوم** (للشيخ صفي الرحمن المباركفوري) |
| `(ديوان)` | **ديوان الإمام الشافعي** |
| `(توحيد)` / `(فتح الباري)` | **فتح الباري شرح صحيح البخاري - كتاب التوحيد** |
| `(روضة)` / `(مختصر الروضة)` | **شرح مختصر الروضة** |
| `(أصول)` / `(لب الأصول)` / `(لب)` | **لب الأصول** |
| `(معاملات)` / `(دليل)` | **دليل الطالب (كتاب البيع)** |
| `(أسماء)` | **الأسماء الحسنى** |

## 4. تكامل خط الإنتاج (NotebookLM / Gemini + Tafreegh Skill)

### المرحلة التحضيرية
- استخراج المتن الأصلي مشكولاً برسم المصحف وضبط الحركات ككتلة نصية محققة تامة (يُشكل هذا المُخرج `<matn_source>`).
- استخراج مسودات التفريغ الأولية عبر **Gemini Notebook** حصرياً.
- تكتمل هذه المرحلة بوجود ملف `<matn_source>` في مجلد `01_Matn_Sources` وملف التفريغ الخام في مجلد `02_Raw_Inputs`.

### المرحلة التنفيذية
- **التنفيذ**: يُنفَّذ التفريغ مع حصر التنسيق ونسب الحوار على الكلام المنطوق حصرياً، بالاعتماد على مهارة `/tafreegh`.
- تكتمل هذه المرحلة بالتحقق من تغطية كامل نص المسودة الخام واستيعاب كافة فقراتها في المخرج النهائي.

### حلقة التعلم المستمر وهندسة الفروقات (Diff Loop)
- يُحفظ ملف الذكاء الاصطناعي الوسيط بصيغة `.md` فور الانتهاء من تصدير الوورد داخل `03_AI_Outputs` مع إضافة لاحقة `_AI.md`.
- يقوم المستخدم بنقل ملف الوورد بعد مراجعته وتعديله إلى مجلد `(تم التسليم)` داخل مجلد المادة في `OneDrive`.
- تُقارن النصوص المراجعة عبر `read_docx.py` مع ملف `_AI.md` لالتقاط قاموس العامية بدقة.
- تكتمل هذه المرحلة باستخراج الفروقات بنجاح وحفظها كملفات تدريب في `04_Training_Data/`.

## 5. بروتوكول تسليم الجلسات وحفظ السياق (Session Handoff Protocol)
- **المستقر الدائم لوثيقة التسليم:** تُحفظ وثيقة التسليم دائمًا في جذر مجلد المشروع باسم `handoff.md` (`Tafreegh_Project/handoff.md`).
- **استئناف العمل:** يعتمد الوكيل في الجلسات الجديدة على قراءة `@handoff.md` لتحديد محطة التنفيذ التالية واسترجاع سياق العمل.