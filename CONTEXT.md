# سياق مشروع تفريغ وتحقيق الدروس الشرعية (الدوري / The Curriculum)

## 1. هوية المشروع والمحاضر
- **المحاضر**: فضيلة الشيخ أسامة (نادراً ما يذكر اسمه صراحة).
- **المنهج والعلماء المكثر من النقل عنهم**: أئمة السلف، وبخاصة شيخ الإسلام ابن تيمية والإمام ابن القيم.
- **طبيعة العمل**: تفريغ مسودات Gemini Notebook، مطابقة متون، استدراك السقط، توحيد العامية المصرية، وإعداد مخرجات مطبعية لبرنامج Microsoft Word.

---

## 2. هيكلة المجلدات (The 4-Folder Architecture)

يعتمد المشروع على 4 مجلدات رئيسية باللغة الإنجليزية لتنظيم دورة حياة البيانات:
1. `01_Matn_Sources`: يحتوي على ملفات المتون الشرعية الأصلية المعتمدة (مثل زاد المعاد، صيد الخاطر) بصيغة Markdown لاستخدامها كمرجع موثوق للـ Blind Literalism.
2. `02_Raw_Inputs`: يحتوي على مسودات التفريغ الخام المستخرجة من Gemini Notebook قبل معالجتها.
3. `03_AI_Outputs`: الأرشيف الداخلي للمشروع؛ تُحفظ فيه المخرجات الأولية للذكاء الاصطناعي كملفات Word (`.docx`) وملفات `.md` خام للرجوع إليها.
4. `04_Training_Data`: مساحة التدريب النهائية؛ يحتوي على ملفات `.md` عالية الجودة التي صُححت واستُخرجت من مجلد `(تم التسليم)` بعد مراجعة المستخدم، لتُستخدم في تعليم الذكاء الاصطناعي (Diff Loop).

---

## 3. جدول توجيه واستدعاء المتون (Dual-Mode Routing Table)

يعتمد استدعاء المتن في المنظومة على الكلمات الدلالية في عنوان المحاضرة:

### النمط الأول: استدعاء المتن من الذاكرة الشاملة (Memory Recall Mode - Shamela)
عند ورود هذه الكلمات، يُستدعى المتن مباشرة من أمهات الكتب المعتمدة:
* `(زاد)` -> **زاد المعاد في هدي خير العباد** (للإمام ابن القيم).
* `(صيد)` -> **صيد الخاطر** (للإمام ابن الجوزي).
* `(رياض)` -> **رياض الصالحين** (للإمام النووي / مع الاستئناس بدليل الفالحين).
* `(سيرة)` -> **الرحيق المختوم** (للشيخ صفي الرحمن المباركفوري / طبعة الأوقاف القطرية).
* `(ديوان)` -> **ديوان الإمام الشافعي**.
* `(توحيد)` أو `(فتح الباري)` -> **فتح الباري شرح صحيح البخاري - كتاب التوحيد** (لابن حجر) [الترويسة الرسمية: فتح الباري (كتاب التوحيد)].
* `(روضة)` أو `(شرح مختصر الروضة)` -> **شرح مختصر الروضة**.

### النمط الثاني: ملفات المتون المرفوعة (Uploaded Docs Mode)
تُستدعى نصوص المتن من ملفات الوورد المرفوعة في بيئة العمل عند ورود الكلمات التالية:
* `(لب)` أو `(أصول)` أو `(لب الأصول)` -> كتاب لب الأصول.
* `(معاملات)` أو `(دليل)` -> **دليل الطالب (كتاب البيع)**.
* `(أسماء)` -> كتب الأسماء والصفات والعقيدة [الترويسة الرسمية: الأسماء الحسنى].

---

## 3. تكامل خط الإنتاج (NotebookLM / Gemini + Tafreegh Skill)

1. **المرحلة التحضيرية (NotebookLM - الدوري):**
   - استخراج المتن الأصلي مشكولاً بالكامل برسم المصحف وضبط الحركات.
   - استدراك السقط بأسلوب (Style A) بحيث يكون المتن كتلة نصية محققة تامة.
   - المخرج من هذه المرحلة يمثل `<matn_source>`.
   - مسودات التفريغ الأولية تُستخرج عبر **Gemini Notebook** (إلغاء الاعتماد على Whisper).

2. **المرحلة التنفيذية (مهارة تفريغ - Tafreegh Skill):**
   - **النمط المزدوج لمعالجة المتن (Dual-Mode Architecture)**:
     - **النمط الأساسي (Standard Mode)**: مطابقة مسودة التفريغ مع المتن الأصلي `<matn_source>` بنظام **الالتزام الحرفي الأعمى (Blind Literalism)**، والتشكيل التام للمتن المقتبس، والتسويد لكافة مواضع المتن.
     - **النمط الاحتياطي (Heuristic Fallback Mode)**: يُفعَّل حصرياً عند غياب `<matn_source>`؛ بالاستكشاف اللغوي الذاتي للمتن عبر مفاتيح القراءة (`قال`، `بيقول`)، والتفريق الحاسم بين الفصحى المحضة 100% والشرح العامي، وكتابة المتن بدون تشكيل في سطر مستقل بين قوسين `**(نص المتن)**`، مع التحوط الصارم عند الشك.
   - التناوب بين قراءة المتن وشرح الشيخ بالعامية الموحدة.
   - **الفصل التفاعلي لكلام الطلاب (Active Dialogue Separation)**: عزل كافة مداخلات وأسئلة الطلاب في أسطر مستقلة تبدأ بـ `طالب: `، وحظر دمجها في كلام الشيخ.
   - **منع التسويد عن الطالب**: وسم `طالب: ` وكلام الطالب بالخط العادي دائماً، والخط العريض (Bold) مخصص حصرياً للمتن.
   - **معايير التمييز بين كلام الطلاب وتنبيهات الشيخ البلاغية**:
     - **يُنسب للطالب (`طالب: `)**: إجابات الأسئلة الموجهة للمجلس، المناقشات والاستدراكات، الردود على نداء الشيخ (`يا فلان`).
     - **قواعد الصوت غير المسموع**:
       - الصمت التام (لم يُسمع شيء نهائياً): يُكتب بدون أقواس `طالب: صوت غير مسموع.`
       - الانقطاع بعد كلمة أو نصف جملة: يُكتب بأقواس `طالب: كلمة (صوت غير مسموع).`
       - قاعدة الإدراج الاستباقي (للتنبيهات المعروفة): عند ظهور تنبيه محادثة معروف (`نعم؟`، `بتقول إيه؟`، `يا فلان` مع وقفة) في **نهاية فكرة** (ليس بلاغياً وسط الكلام) متبوعاً بانتقال في الموضوع، يُدرج تلقائياً `طالب: صوت غير مسموع.` في سطر مستقل بعد جملة الشيخ.
       - قاعدة عدم التخمين (للمواقف المجهولة): إذا لم يرد أي تنبيه معروف، يُدمج النص ولا يُخمن. يقوم الـ Diff Loop تلقائياً باقتراح العبارات السابقة كتنبيهات مرشحة جديدة (تتطلب تأكيد المستخدم).
     - **يظل كلاماً للشيخ**: الأسئلة التقريرية المتبوعة بجواب فوري ("هل هو واجب؟ لا، مش واجب")، الافتراض الجدلي ("تقول لي وأقول لك")، وألفاظ التنبيه الأحادية ("واضح؟"، "خلي بالك").
     - **الأصوات البيئية**: تُتجاهل جميع الأصوات غير المتعلقة بكلام الطلاب (انقطاع الصوت، رنين الهاتف، ضوضاء خلفية). فقط كلام الطالب يستحق النسب والتنسيق.
   - تنسيق الأسطر ومطابقة مواصفات `Word base.docx` بدقة.

3. **حلقة التعلم المستمر وهندسة الفروقات (Diff Loop Architecture):**
   - **أساس المقارنة المرجعي (The Perfect Baseline)**: الملف الوسيط بصيغة (`.md`) الذي يُولده الذكاء الاصطناعي هو الأساس المثالي للمقارنة (Plain-text). لا يتم إجراء الـ Diff بين ملفات `.docx` مباشرة لصعوبة قراءتها برمجياً (XML noise).
   - **الاحتفاظ التلقائي بالملف الوسيط**: يُحفظ ملف الذكاء الاصطناعي الوسيط فور الانتهاء من تصدير الوورد داخل مجلد `02_Raw_Inputs` مع إضافة لاحقة `_AI.md` (مثال: `2026-08-22_لب_الأصول_AI.md`). هذا يحمي مسودة Gemini الأصلية (لمطابقتها كأصل) ويوفر نسخة الذكاء الاصطناعي لمعرفة ما عجز النموذج عن إصلاحه.
   - **خطوات التعلم**: تُستخرج النصوص من ملف الوورد النهائي المراجع (بواسطة المستخدم) عبر `read_docx.py` وتُقارن حصرياً مع ملف `_AI.md` لالتقاط التعديلات البشرية الجديدة (كقاموس العامية) بدقة متناهية.

---

## 4. خريطة تصدير ملفات Word وتوزيع المجلدات (Dual Export Routing)

تُصدَّر مخرجات التفريغ تلقائياً كملفي Microsoft Word (`.docx`) مستقلين:

### أ. نسخة المستخدم الأساسية (OneDrive)
- **المسار الجذري**: `C:\Users\L\OneDrive\1. دوري`
- **تسمية الملف**: بالتاريخ فقط `[YYYY-MM-DD].docx` (مثال: `2026-09-18.docx`).
- **خريطة المجلدات الحصرية**:
  1. `اسماء` / `أسماء` -> مجلد `1. الاسماء الحسنى`
  2. `رياض` -> مجلد `1. رياض الصالحين`
  3. `سيرة` -> مجلد `1. سيرة (الرحيق المختوم)`
  4. `معاملات` / `دليل` -> مجلد `1. معاملات`
  5. `توحيد` / `فتح الباري` -> مجلد `2. التوحيد` [الترويسة: فتح الباري (كتاب التوحيد)]
  6. `لب` / `لب الاصول` / `اصول` / `الاصول` -> مجلد `2. لب الاصول`
  7. `ديوان` -> مجلد `ديوان الشافعي`
  8. `زاد` -> مجلد `زاد المعاد`
  9. `روضة` / `مختصر الروضة` -> مجلد `شرح مختصر الروضة`
  10. `صيد` -> مجلد `صيد الخاطر`
  - **قاعدة الإنشاء التلقائي**: أي مادة جديدة خارج هذه المواد العشر، يُنشأ لها مجلد باسم المادة داخل `1. دوري` تلقائياً ليصبح مستقرها الدائم.
- **التسليم والتدريب (The Handoff Workflow)**:
  - يقوم المستخدم بنقل ملف الوورد بعد مراجعته وتعديله إلى مجلد فرعي باسم `(تم التسليم)` داخل مجلد المادة.
  - تقوم الأداة الآلية بجلب الملف، واستخراج النصوص بالتنسيق، وحفظه كملف `[Date]_[Subject].md` داخل مجلد `04_Training_Data` للتدريب. يُحفظ ملف الوورد الأصلي بأمان داخل مجلد `(تم التسليم)`، ويتم تخطيه تلقائياً في المرات القادمة بمجرد وجود ملف الـ `.md` المقابل.

### ب. نسخة المشروع المحلي (Internal Archive)
- **المسار**: `03_AI_Outputs/[YYYY-MM-DD]_[اسم_المادة].docx`
- **الغرض**: أرشفة برمجية داخلية للمشروع لاستخدامها في حلقة التعلم الآلي وتطوير الـ Skill (`Diff Loop`) دون إشغال المستخدم بها.

---

## 5. أدوات خط الإنتاج الموحدة والتصدير (Automation & Pipeline Tools)

تعتمد المنظومة على حزمة أدوات برمجية موحدة داخل `.agents/skills/tafreegh/scripts/` مغطاة باختبارات آلية:

1. **محول وموزع مستندات الوورد الموحد (`export_docx.py`):**
   - الأداة الرسمية الوحيدة لتوليد ملفات Microsoft Word بصيغة `.docx`.
   - **المرجع الشكلي الأساسي**: يستمد مواصفاته بالكامل وراثة من ملف `Word base.docx`:
     - هوامش الصفحة: 0.5 بوصة من جميع الجهات (0.5 in).
     - الخط: Traditional Arabic بحجم 18 لكافة النصوص.
     - اللون: أسود موحد موروث طبيعياً من قالب Word base.docx.
     - المتن: يُبرز بالخط العريض (Bold) فقط عبر `<w:b/>` و `<w:bCs/>`، مع حظر التسويد عن وسوم الطلاب.
     - الترويسة القياسية (أعلى الصفحة في المنتصف):
       ```text
       بسم الله الرحمن الرحيم
       المادة: [اسم المادة المعتمد فقط]
       [YYYY-MM-DD]
       ```
   - **التصدير المزدوج التلقائي**: يصدر الملف فوراً وبأمر واحد لنسخة الـ OneDrive (موزعة على مجلدات المواد العشر) ونسخة أرشيف المشروع الداخلي `03_AI_Outputs/`.

2. **أداة التقطيع الذاتي للمحاضرات (`chunk_transcript.py`):**
   - تُستخدم عند استلام نصوص محاضرات طويلة تتجاوز الحجم الأمثل للنموذج.
   - تقسم النص آلياً عند الوقفات اللغوية وفواصل الجمل العربية إلى مقاطع محكمة (1200 إلى 1500 كلمة لكل جزء) دون قطع العبارات.

3. **قارئ ملفات الوورد لدورة التعلم (`read_docx.py` & `fetch_training_data.py`):**
   - أداة `fetch_training_data.py` تبحث تلقائيًا في مجلدات `(تم التسليم)` على OneDrive.
   - تستخرج النصوص بجميع التنسيقات عبر `read_docx.py` من ملفات الوورد.
   - تحفظ ملفات التدريب في `04_Training_Data/`.

4. `chunk_transcript.py` & `export_docx.py`: المعالجة الذاتية الشاملة للمقاطع
   - `Dynamic Assessment`: يقرأ الوكيل الآلي مسار المحاضرة المقطعة ويحصي عدد المقاطع دون التقيد برقم ثابت.
   - `Parallel Subagents`: يُشغل الوكيل الرئيسي وكلاء فرعيين في الخلفية دفعة واحدة. كل وكيل يعالج مقطعاً واحداً باستقلالية تامة مطبقاً قواعد `Blind Literalism` ومهارة `/tafreegh`.
   - `Stitching`: ينتظر الوكيل الرئيسي انتهاء جميع الوكلاء، ثم يدمج مخرجاتهم بالترتيب الصحيح في ملف `_AI_full.md` واحد.
   - `Final Export`: يُنفذ الوكيل الرئيسي سكربت `export_docx.py` مرة واحدة على الملف المدمج لتسليم المستخدم ملف `Word` نهائي متكامل في محادثة واحدة وبدون تدخل مرحلي.

---

## 6. `Session Handoff Protocol`: بروتوكول تسليم الجلسات وحفظ السياق

- **المستقر الدائم لوثيقة التسليم:** تُحفظ وثيقة التسليم دائمًا في جذر مجلد المشروع باسم:
  `handoff.md` (`Tafreegh_Project/handoff.md`).
- **آلية الاستدعاء في المحادثات الجديدة:** في أي جلسة عمل جديدة، يكفي كتابة رمز الإشارة المباشر:
  `@handoff.md`
  في صندوق المحادثة ليتعرف الوكيل فورًا على كافة إعدادات المشروع، وأدوات خط الإنتاج، والقواعد اللغوية الصارمة، والمحطة التالية للتنفيذ.
- `Universal Prompt Template`: عند بدء تفريغ محاضرة جديدة تستدعي المعالجة الشاملة للمقاطع، يتم استخدام القالب المحفوظ في `.agents/prompts/universal_prompt.md`. ينسخ المستخدم القالب الشامل في محادثة جديدة مع توفير مسارات الملفات، ليبدأ الوكيل الآلي العمل مباشرة.

